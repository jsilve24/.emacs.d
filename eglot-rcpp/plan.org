* eglot-rcpp maintenance

** Deferred follow-up

*** TODO Revisit richer semantic cross-project reference aggregation
Definitions, symbol search, and references now have clean package-scoped
fallbacks, but references still rely on current-server results plus textual
project search rather than a truly semantic multi-server index.

*** TODO Explore lightweight inline-C++ helper commands for R buffers
Inline workflows such as `Rcpp::sourceCpp(code = ...)` remain out of scope for
transparent multi-server support. Future work should stay command-oriented and
avoid pretending an ordinary R buffer has one unified server.
